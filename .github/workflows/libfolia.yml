name: C/C++ CI

on:
  schedule:
    - cron: "0 3 4 * 5"  # run test once a month
  push:
    branches:
      - master
      - PROPS
    paths:
      - configure.ac
      - 'src/**'
      - 'include/**'
      - .github/workflows/libfolia.yml

  pull_request:
    branches: [master]

jobs:
  notification:
    runs-on: ubuntu-latest
    name: Notify start to gitlama
    outputs:
      branch: ${{ steps.extract_branch.outputs.branch }}
    steps:
      - name: Extract Branch name
        shell: bash
        run: echo "branch=$(echo ${GITHUB_REF#refs/heads/})" >> $GITHUB_OUTPUT
        id: extract_branch
      - name: IRC notification
        uses: Gottox/irc-message-action@v2
        with:
          server: irc.uvt.nl
          channel: '#gitlama'
          nickname: GitHub
          message: >
            ${{ github.actor }} started a build of
            ${{ github.event.repository.name }}
            [${{ steps.extract_branch.outputs.branch }}]
            "${{ github.event.head_commit.message }}"

  build:
    needs: notification
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-14]
        compiler: [g++-12, clang++]

    steps:
      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.12.1
        with:
          access_token: ${{ github.token }}

      - uses: actions/checkout@v4.1.1
      - name: Install Build Environment
        run: |
             if [ "$RUNNER_OS" == "Linux" ]; then
                sudo apt-get install pkg-config autoconf-archive
             else
                brew upgrade;
                brew install pkg-config
                brew install libtool
                brew install autoconf-archive
                brew install autoconf
                brew install automake
             fi
      - name: Install Dependencies
        run: |
             if [ "$RUNNER_OS" == "Linux" ]; then
                sudo apt-get install libicu-dev libxml2-dev libbz2-dev;
                sudo apt-get install zlib1g-dev cppcheck
             else
                brew install libxml2
                brew install bzip2
                brew install zlib
                brew install cppcheck
             fi
      - name: Configure CppCheck
        run: |
             if [ "$RUNNER_OS" == "Linux" ]; then
                cpc_opts="--enable=all --inline-suppr --force \
                         -I include -I /usr/local/include \
                         --std=c++17 --quiet --error-exitcode=0"
             else
                cpc_opts="--enable=all --inline-suppr --force \
                          --check-level=exhaustive \
                          --suppress=missingInclude \
                          --suppress=missingIncludeSystem \
                          --std=c++17 --quiet --error-exitcode=0"
             fi
             echo "cpc_opts=$cpc_opts" >> $GITHUB_ENV
      - name: compiler ID
        run: |
          id=$(echo ${{matrix.compiler}} | cut -d\+ -f1)
          echo "id=$id" >> $GITHUB_OUTPUT
      - name: install TiccUtils
        id: ticcutils
        env:
          CXX: ${{ matrix.compiler }}
        run: |
          git clone https://github.com/LanguageMachines/ticcutils;
          cd ticcutils;
          bash bootstrap.sh;
          ./configure;
          make;
          sudo make install;
          cd ..;
        continue-on-error: true
      - name: get status ticcutils
        if: ${{ steps.ticcutils.outcome != 'success' }}
        run: echo "action_status=ticcutils" >> $GITHUB_ENV
      - name: Static Code-check
        run: cppcheck ${{ env.cpc_opts }} .
      - name: bootstrap
        run: sh bootstrap.sh
      - name: configure
        id: configure
        env:
          CXX: ${{ matrix.compiler }}
        run: ./configure
        continue-on-error: true
      - name: get status configure
        if: ${{ steps.configure.outcome != 'success' }}
        run: echo "action_status=configure" >> $GITHUB_ENV
      - name: make
        id: make
        if: ${{ env.action_status == '' }}
        run: make
        continue-on-error: true
      - name: get status make
        if: ${{ steps.make.outcome != 'success' }}
        run: echo "action_status=make" >> $GITHUB_ENV
      - name: install
        id: install
        if: ${{ env.action_status == '' }}
        run: sudo make install
        continue-on-error: true
      - name: get status install
        if: ${{ steps.install.outcome != 'success' }}
        run: echo "action_status=install" >> $GITHUB_ENV
      - name: make check
        id: check
        env:
          CXX: ${{ matrix.compiler }}
          LD_LIBRARY_PATH: $LD_LIBRARY_PATH:/usr/local/lib
        if: ${{ env.action_status == '' }}
        run: make check
        continue-on-error: true
      - name: get status check
        if: ${{ steps.check.outcome != 'success' }}
        run: echo "action_status=check" >> $GITHUB_ENV
      - name: Show log of failed check
        if: ${{ steps.check.outcome != 'success' }}
        run: |
          cat src/test-suite.log
        continue-on-error: true
      - name: Notify IRC of failure
        if: ${{ env.action_status != '' }}
        uses: Gottox/irc-message-action@v2
        with:
          server: irc.uvt.nl
          channel: '#gitlama'
          nickname: GH-${{ runner.os }}-${{ steps.compiler.outputs.id }}
          message: "libfolia [${{ needs.notification.outputs.branch }}] make check with ${{ matrix.compiler }} on ${{ matrix.os }}: \u00034FAILED\u0003 in step ${{ env.action_status }}"

      - name: install foliatest
        id: foliatest
        if: ${{ env.action_status == '' }}
        env:
          CXX: ${{ matrix.compiler }}
        run: |
          git clone --branch ${{ needs.notification.outputs.branch }} --depth=1 https://github.com/LanguageMachines/foliatest.git
          cd foliatest;
          bash bootstrap.sh;
          ./configure;
          make;
          sudo make install;
          cd ..;
        continue-on-error: true
      - name: get status foliatest
        if: ${{ env.action_status == '' }} && ${{ steps.foliatest.outcome != 'success' }}
        run: echo "action_status=foliatest" >> $GITHUB_ENV

      - name: install FoliaPY
        if: ${{ env.action_status == '' }}
        run: |
          git clone https://github.com/proycon/foliapy;
          cd foliapy;
          # get folia-repo with examples
          git submodule init && git submodule update;
          cd ..

      - name: run foliatests
        id: foliatests
        if: ${{ env.action_status == '' }}
        env:
          FOLIAPATH: ../foliapy/folia-repo
          EXPECT: 0
          LD_LIBRARY_PATH: $LD_LIBRARY_PATH:/usr/local/lib
        run: |
          cd foliatest;
          ./src/testaction.sh;
          TEST_STAT=$(cat status.tmp)
          echo "test_status=$TEST_STAT" >> $GITHUB_ENV
        continue-on-error: true
      - name: Notify IRC of failure
        if: ${{ env.test_status != '0' }}
        uses: Gottox/irc-message-action@v2
        with:
          server: irc.uvt.nl
          channel: '#gitlama'
          nickname: GH-${{ runner.os }}-${{ steps.compiler.outputs.id }}
          message: "libfolia [${{ needs.notification.outputs.branch }}] build with ${{ matrix.compiler }} by ${{ github.actor }} on ${{ matrix.os }}: \u00034FAIL\u0003 in foliatests (${{ env.test_status }} errors)"

      - name: Notify IRC of succes
        if: ${{ env.action_status == '' }} && ${{ env.test_status == '0' }}
        uses: Gottox/irc-message-action@v2
        with:
          server: irc.uvt.nl
          channel: '#gitlama'
          nickname: GH-${{ runner.os }}-${{ steps.compiler.outputs.id }}
          message: "libfolia [${{ needs.notification.outputs.branch }}] build with ${{ matrix.compiler }} by ${{ github.actor }} on ${{ matrix.os }}: \u00033SUCCESS\u0003"
